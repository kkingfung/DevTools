# Gameplay Live Tuning Dashboard

**「数値変更 → 即反映 → 影響を比較」を超高速化**

ゲームプレイパラメータをリアルタイムで調整し、変更履歴を追跡、安全性をベンチマークするUE5エディタプラグイン。

## 課題

バランス調整で陥りがちな問題：

- **何を変えたか忘れる**: 複数パラメータを調整した後、元の値がわからない
- **影響範囲が見えない**: 1つの変更が他にどう影響するか把握困難
- **試行錯誤が遅い**: 変更 → ビルド → テストのサイクルが長い
- **危険な値を見逃す**: 閾値を超えた設定がそのまま本番に

## 価値

- **"何を変えたか忘れる"を根絶**
- **調整の"思考→試行→評価"ループを高速化**
- **PVE/BALANCEミーティングの時間が半分に**

## 機能

### レイヤー管理
パラメータを論理的なレイヤーで整理：

| レイヤー | 用途例 |
|---------|--------|
| キャラクター | HP、移動速度、ジャンプ力 |
| 武器 | ダメージ、発射速度、リロード時間 |
| スキル | クールダウン、消費MP、効果範囲 |
| ステージ | 敵配置数、リスポーン時間、難易度 |
| AI | 反応速度、攻撃頻度、逃走閾値 |
| 経済 | ドロップ率、価格、報酬 |
| カスタム | 自由に定義 |

### ライブ変更 & 即反映
- ゲーム実行中にパラメータを変更
- 変更は即座にゲーム内に反映
- リビルド不要

### 変更履歴追跡
```
┌──────────────────────────────────────────────────┐
│ 変更履歴                                          │
├──────────────────────────────────────────────────┤
│ 14:32:15 │ Character.Health │ 100 → 150 │ +50.0% │
│ 14:31:42 │ Weapon.Damage    │ 25 → 35   │ +40.0% │
│ 14:30:58 │ Skill.Cooldown   │ 5 → 3     │ -40.0% │
└──────────────────────────────────────────────────┘
```

- 全変更を時刻付きで記録
- Before / After 比較表示
- 変化率（%）を自動計算
- Undo / Redo 対応

### セッション管理
- 調整セッションをグループ化
- セッション単位で変更を追跡
- セッション間の比較が可能

### 安全ベンチマーク
閾値を超えた危険な値を自動検出：

```
┌──────────────────────────────────────────────────┐
│ 安全ベンチマーク                    [実行]        │
├──────────────────────────────────────────────────┤
│ ✗ FAILED                                         │
│ チェック: 15 パラメータ                            │
│                                                  │
│ 🔴 致命的警告: 2                                  │
│   • 最大体力: +150.0%                            │
│   • 基礎ダメージ: +140.0%                        │
│                                                  │
│ 🟡 警告: 3                                       │
│   • 移動速度: +50.0%                             │
│   • クールダウン: -60.0%                         │
└──────────────────────────────────────────────────┘
```

### プリセット
- 現在のパラメータセットを保存
- 名前付きプリセットで管理
- ワンクリックで適用

### インポート/エクスポート
- JSON形式でエクスポート
- チーム間でパラメータセットを共有
- バージョン管理システムと連携可能

## UI構成

```
┌─────────────────────────────────────────────────────────────────────┐
│ [セッション名] [開始] [終了]  │  [↩ Undo] [Redo ↪]  [プリセット保存] │
├───────────────────────────────┴─────────────────────────────────────┤
│ [キャラクター] [武器] [スキル] [ステージ] [AI] [経済] [カスタム]        │
│   15 params     8 params (🟡2)   ...                                 │
├────────────────────┬─────────────────────┬──────────────────────────┤
│ パラメータ          │ 詳細                 │ Before vs After         │
│                    │                     │                          │
│ 🟢 最大体力    [100]│ 最大体力             │ 最大体力: 100→150 (+50%) │
│ 🟡 移動速度    [900]│ キャラクターの最大HP  │ 移動速度: 600→900 (+50%) │
│ 🟢 ジャンプ力  [400]│                     │                          │
│                    │ 現在値: 100.0        ├──────────────────────────┤
│                    │ デフォルト: 100.0    │ 安全ベンチマーク [実行]    │
│                    │                     │                          │
│                    │ 閾値設定             │ ✓ PASSED / ✗ FAILED     │
│                    │ 警告: 50〜500        │ 🔴 致命的: 0             │
│────────────────────│ 危険: 10〜1000       │ 🟡 警告: 2               │
│ 変更履歴            │                     │                          │
│ 14:32:15 HP 100→150│                     │                          │
└────────────────────┴─────────────────────┴──────────────────────────┘
```

## 使い方

### 1. ウィンドウを開く
```
Window > Live Tuning Dashboard
```

### 2. パラメータを登録
```cpp
// ブループリントまたはC++から
UTuningSubsystem* Tuning = UTuningSubsystem::Get();

FTuningParameter Param;
Param.ParameterId = FName("Character.Health");
Param.DisplayName = TEXT("最大体力");
Param.Layer = ETuningLayer::Character;
Param.Category = TEXT("基本ステータス");
Param.CurrentValue.ValueType = ETuningValueType::Float;
Param.CurrentValue.FloatValue = 100.0f;
Param.DefaultValue = Param.CurrentValue;

// 閾値設定
Param.Threshold.MinValue = 50.0f;
Param.Threshold.MaxValue = 500.0f;
Param.Threshold.CriticalMinValue = 10.0f;
Param.Threshold.CriticalMaxValue = 1000.0f;
Param.Threshold.MaxChangePercent = 50.0f;

// オブジェクトへの自動適用（オプション）
Param.TargetObjectPath = TEXT("/Game/Characters/BP_Hero.BP_Hero_C");
Param.TargetPropertyName = TEXT("MaxHealth");

Tuning->RegisterParameter(Param);
```

### 3. 値を変更
UIから直接スピンボックスで変更、または：

```cpp
Tuning->SetFloatValue(FName("Character.Health"), 150.0f, TEXT("バランステスト"));
```

### 4. セッションを管理
```cpp
// セッション開始
Tuning->StartSession(TEXT("バランス調整 v1.2"));

// ... パラメータ変更 ...

// セッション終了
Tuning->EndCurrentSession();
```

### 5. ベンチマーク実行
```cpp
FTuningBenchmarkResult Result = Tuning->RunSafetyBenchmark();
if (!Result.bPassed)
{
    // 致命的な問題あり
    for (const FTuningComparison& Warning : Result.CriticalWarnings)
    {
        UE_LOG(LogTemp, Error, TEXT("Critical: %s changed by %.1f%%"),
            *Warning.Parameter.DisplayName,
            Warning.PercentChange);
    }
}
```

## Blueprint API

```cpp
// パラメータ取得
UFUNCTION(BlueprintCallable)
bool GetParameter(FName ParameterId, FTuningParameter& OutParameter);

// 値の設定
UFUNCTION(BlueprintCallable)
bool SetFloatValue(FName ParameterId, float Value, const FString& Comment);

// デフォルトにリセット
UFUNCTION(BlueprintCallable)
bool ResetToDefault(FName ParameterId);

// Undo/Redo
UFUNCTION(BlueprintCallable)
bool UndoLastChange();
bool RedoChange();

// 比較
UFUNCTION(BlueprintCallable)
TArray<FTuningComparison> CompareWithDefault();

// ベンチマーク
UFUNCTION(BlueprintCallable)
FTuningBenchmarkResult RunSafetyBenchmark();
```

## イベント

```cpp
// パラメータ変更時
UPROPERTY(BlueprintAssignable)
FOnParameterChanged OnParameterChanged;

// 警告発生時
UPROPERTY(BlueprintAssignable)
FOnWarningTriggered OnWarningTriggered;

// セッション変更時
UPROPERTY(BlueprintAssignable)
FOnSessionChanged OnSessionChanged;
```

## 警告レベル

| レベル | 色 | 条件 |
|--------|-----|------|
| None | 🟢 緑 | 閾値内 |
| Info | 🔵 青 | 情報のみ |
| Warning | 🟡 黄 | 閾値超過 |
| Critical | 🔴 赤 | 危険閾値超過 |

## データ構造

### FTuningParameter
```cpp
USTRUCT()
struct FTuningParameter
{
    FName ParameterId;           // 一意識別子
    FString DisplayName;         // 表示名
    FString Description;         // 説明
    ETuningLayer Layer;          // レイヤー
    FString Category;            // カテゴリ
    FTuningValue CurrentValue;   // 現在値
    FTuningValue DefaultValue;   // デフォルト値
    FTuningThreshold Threshold;  // 安全閾値
    FString TargetObjectPath;    // 適用先オブジェクト
    FString TargetPropertyName;  // 適用先プロパティ
};
```

### FTuningValue
```cpp
USTRUCT()
struct FTuningValue
{
    ETuningValueType ValueType;  // Float/Integer/Boolean/Vector
    float FloatValue;
    int32 IntValue;
    bool BoolValue;
    FVector VectorValue;
};
```

## ワークフロー例

### バランスミーティング
1. セッション「Balance Meeting 2024-01-15」開始
2. 議論しながらパラメータ調整
3. Before/After 比較でチーム確認
4. ベンチマーク実行で安全性確認
5. 問題なければプリセット保存
6. セッション終了

### A/Bテスト準備
1. プリセット「Aggressive」作成（攻撃力↑防御力↓）
2. プリセット「Defensive」作成（防御力↑攻撃力↓）
3. テスト時にプリセット切り替え
4. 結果を比較

## 動作要件

- Unreal Engine 5.0+
- エディタ専用プラグイン

## インストール

1. `GameplayLiveTuningDashboard`フォルダをプロジェクトの`Plugins`ディレクトリにコピー
2. プロジェクトを再起動
3. `Window > Live Tuning Dashboard`でツールを開く

## ライセンス

MIT License
